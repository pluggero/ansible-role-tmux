---
tmux_plugins:
  - name: "vim-tmux-navigator"
    namespace: "christoomey"
tmux_prefix: "C-a"
tmux_key_bindings:
  - key: "C-l"
    command: "send 'C-l'"
  - key: "C-y"
    flags: "-n"
    command: >-
      copy-mode \;
      send -X cursor-up \;
      send -X cursor-up \;
      send -X search-backward "$USER"
